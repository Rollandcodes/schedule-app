{
  "$schema": "https://json.schemastore.org/package.json",
  "private": true,
  "name": "@schedule/ics-feed",
  "version": "0.0.0",
  "main": "./index.ts",
  "dependencies": {
    "@schedule/lib": "workspace:*",
    "@schedule/prisma": "workspace:*",
    "@schedule/ui": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "react-dom": "^18.0.0 || ^19.0.0",
    "react-hook-form": "^7.0.0"
  },
  "devDependencies": {
    "@schedule/types": "workspace:*"
  },
  "description": "Import events from an ICS Feed into Cal.diy."
}
